"""
this program will greet you.
"""

use GreetHello
use VariantMvc
# use GenericProject

class MyProgram

	def main
		view = ConsoleView()
		ctrl = GreetController(view)
		ctrl.performAction("greet")
		ctrl.performAction("exit")
